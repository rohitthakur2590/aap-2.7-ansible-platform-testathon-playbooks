---
# Your Red Hat username — used to namespace all test resources.
#
# NOTE: The canonical place to set this is group_vars/all.yml.
# This file is still loaded by vars_files in individual playbooks as a fallback.
# Keep both in sync.

rh_username: "rothakur"
